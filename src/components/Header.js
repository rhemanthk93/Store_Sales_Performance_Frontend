import React from 'react';
import './Header.css';

const Header = () => {
    return (
        <header className="header">
            <h1>Sales Order Analytics Dashboard</h1>
        </header>
    );
};

export default Header;